// 1 уровень сложности: Имея два отсортированных массива размера m и n соответственно, вам нужно найти элемент, который будет находиться на k-й позиции в конечном отсортированном массиве.
// Массив 1 - 100 112 256 349 770
// Массив 2 - 72 86 113 119 265 445 892
// к = 7
// Вывод : 256
// Окончательный отсортированный массив -
// 72, 86, 100, 112, 113, 119, 256, 265, 349, 445, 770, 892
// 7-й элемент этого массива равен 256.

// Создайте два указателя, один для первого массива и один для второго массива, и инициализируйте их нулем.
// Сравните элементы, на которые указывают указатели в обоих массивах.
// Выберите меньший из двух элементов и увеличьте соответствующий указатель на 1.
// Повторяйте шаги 2-3 до тех пор, пока не достигнете k-го элемента.
// K-й элемент будет являться результатом.

// function findKthElement(arr1, arr2, k) {
//     let pointer1 = 0; // Указатель для первого массива
//     let pointer2 = 0; // Указатель для второго массива
    
//     while (pointer1 < arr1.length && pointer2 < arr2.length) {
//         if (arr1[pointer1] < arr2[pointer2]) {
//             // Если элемент из первого массива меньше, увеличиваем указатель первого массива
//             pointer1++;
//         } else {
//             // Иначе увеличиваем указатель второго массива
//             pointer2++;
//         }
        
//         k--; // Уменьшаем k на 1
        
//         if (k === 0) {
//             // Если k стало равным 0, значит мы нашли k-й элемент
//             return Math.min(arr1[pointer1], arr2[pointer2]);
//         }
//     }
    
//     // Если один из указателей достиг конца своего массива, продолжаем двигаться по другому массиву
//     while (pointer1 < arr1.length) {
//         pointer1++;
//         k--;
//         if (k === 0) {
//             return arr1[pointer1];
//         }
//     }
    
//     while (pointer2 < arr2.length) {
//         pointer2++;
//         k--;
//         if (k === 0) {
//             return arr2[pointer2];
//         }
//     }
    
//     return -1; // Если k больше, чем общая длина обоих массивов
// }

// const arr1 = [100, 112, 256, 349, 770];
// const arr2 = [72, 86, 113, 119, 265, 445, 892];
// const k = 7;

// const result = findKthElement(arr1, arr2, k);
// console.log(result); // Выведет 256
